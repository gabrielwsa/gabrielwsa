<h2
	class={`flex items-center gap-3 text-3xl font-semibold tracking-tight text-[var(--text-primary)] sm:text-4xl [&>svg]:size-6 [&>svg]:shrink-0 [&>svg]:text-[var(--accent-strong)] ${Astro.props.className ?? ""}`}
>
    <slot />
</h2>
