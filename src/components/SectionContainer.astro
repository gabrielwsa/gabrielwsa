---
const { class: className, id } = Astro.props
---

<section
  id={id}
  data-section={id}
  class={`section ${className} scroll-m-28 mx-auto w-full max-w-6xl px-4 sm:px-6 lg:px-8`}
>
  <slot />
</section>
